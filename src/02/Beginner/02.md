# Persiapan Media

## Menyiapkan Media Instalasi

Setelah kamu mengunduh file ISO T4n OS, langkah berikutnya adalah membuat media instalasi yang bisa digunakan untuk boot (USB, SD Card, atau DVD).

## 1. Membuat USB / SD Card Bootable (Linux)

### Langkah 1: Identifikasi Perangkat

Sebelum menulis ISO, kamu harus tahu **device mana** yang akan digunakan.

Colokkan USB / SD Card, lalu jalankan:

```bash
# fdisk -l
```

Contoh output:

```
Disk /dev/sda: 7.5 GiB, 8036286464 bytes, 15695872 sectors
Disk model: Your USB Device's Model
```

📌 Penjelasan:

* `/dev/sda` = lokasi perangkat
* `7.5 GiB` = ukuran storage
* Gunakan info ini untuk memastikan itu benar USB kamu

💡 Umumnya:

* USB → `/dev/sdX`
* SD Card → `/dev/mmcblkX`

### Langkah 2: Unmount Perangkat

Pastikan perangkat tidak sedang digunakan:

```bash
# umount /dev/sdX
```

Jika muncul:

```
not mounted
```

➡️ Artinya aman, bisa lanjut.

### Langkah 3: Menulis ISO ke USB
#### Software Yang Bisa Digunakan
- [Balena Etcher](https://etcher.balena.io/) : Universal
- [Rufus](https://rufus.ie/id/) : Only Support Windows
- [PopSicle](https://github.com/pop-os/popsicle/releases) : Only Support Linux

Gunakan perintah berikut:

```bash
# dd bs=4M if=/path/to/T4nOS-x86_64.iso of=/dev/sdX status=progress
```

📌 Penjelasan:

* `if=` = lokasi file ISO
* `of=` = target device (USB kamu)
* `bs=4M` = mempercepat proses
* `status=progress` = menampilkan progress

⚠️ **PERINGATAN KERAS:**

> Semua data di USB akan TERHAPUS TOTAL!

### Tunggu Proses Selesai

Output kira-kira seperti ini:

```
377487360 bytes copied, 20 s, 18 MB/s
```

### Langkah 4: Pastikan Data Sudah Ditulis

```bash
$ sync
```

Ini memastikan semua data benar-benar tersimpan sebelum USB dicabut.

## 2. Burning ISO ke CD/DVD

Kalau kamu ingin pakai CD/DVD, gunakan aplikasi berikut:

* [K3b](https://github.com/kde/k3b) : Full-featured CD/DVD/Blu-ray burning and ripping application 
* [Xfburn](https://docs.xfce.org/apps/xfburn/start) : Xfburn is a simple CD/DVD/Blu-ray burning too
* [Brasero](https://wiki.gnome.org/Apps/Brasero) : Brasero is a GNOME application to burn CD/DVD, designed to be as simple as possible.  

📌 Cara umum:

1. Buka aplikasi
2. Pilih "Burn ISO Image"
3. Pilih file ISO T4n OS
4. Klik Burn

⚠️ Catatan:

* CD/DVD lebih lambat dibanding USB
* Kurang responsif saat live boot

## 🚀 Tips Tambahan

* Gunakan USB minimal **8GB**
* Pastikan ISO tidak corrupt (cek SHA256 jika tersedia)
* Gunakan port USB langsung (hindari hub jika bermasalah)
* Jika gagal boot → cek BIOS/UEFI (boot priority)

## 👍 Rekomendasi

Metode terbaik:

* ✅ USB → paling cepat & direkomendasikan
* ⚠️ DVD → hanya alternatif

---
@T4n-Labs
@Gh0sT4n